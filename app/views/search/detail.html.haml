%h1 Result Detail
%hr

%table{:border => 'solid'}
	-@details.each do |a|
		%tr
			-a.each do |k,v|
				%td= k
				%td= v

%br

%button= link_to 'tweetする', search_do_tweet_path(:pid => params[:pid], :text => @details[:Title])
%button= link_to 'Facebookに投稿', search_do_facebook_put_path(:pid => params[:pid], :text => @details[:Title])

%hr

%p= link_to 'top', root_path