%h1 Result Detail
%hr

%table{:border => 'solid'}
	-@details.each do |a|
		%tr
			-a.each do |k,v|
				%td= k
				%td= v

%a= link_to 'tweetする', search_do_tweet_path(:pid => params[:pid], :text => @details[:Title])

%hr

%p= link_to 'top', root_path